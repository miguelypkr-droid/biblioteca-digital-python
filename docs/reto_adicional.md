# Reto Adicional

Elige uno o varios:

1) **Persistencia JSON**
   - Métodos `exportar_json(ruta)` y `cargar_json(ruta)` en `Biblioteca`.
   - Implementa `to_dict()`/`from_dict()` en entidades.

2) **Reportes CSV**
   - Exporta `prestamos_activos` a `prestamos.csv`.

3) **Recomendaciones**
   - Sugerir 3 libros disponibles por género/autor según historial.

4) **Tests unitarios**
   - Agrega casos para errores y esquinas (IDs inválidos, préstamo duplicado, etc.).
